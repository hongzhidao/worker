
Changes with Worker 0.1.0                                        04 Sep 2026

    *) Change: the project was renamed to Worker.

    *) Change: user-facing commands, process titles, default paths, language
       packages, documentation, and distribution metadata now use the Worker
       brand.

    *) Change: the changelog now records Worker releases only; the complete
       NGINX Unit development history remains available in Git.
