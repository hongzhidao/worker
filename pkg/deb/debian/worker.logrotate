/var/log/worker.log {
    daily
    missingok
    rotate 7
    compress
    delaycompress
    nocreate
    notifempty
    su root root
    postrotate
        if [ -f /var/run/worker.pid ]; then
            /bin/kill -SIGUSR1 `cat /var/run/worker.pid`
        fi
    endscript
}
