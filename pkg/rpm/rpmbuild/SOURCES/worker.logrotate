/var/log/worker/*.log {
    daily
    missingok
    rotate 7
    compress
    delaycompress
    nocreate
    notifempty
    postrotate
        if [ -f /var/run/worker/worker.pid ]; then
            /bin/kill -SIGUSR1 `cat /var/run/worker/worker.pid`
        fi
    endscript
}
